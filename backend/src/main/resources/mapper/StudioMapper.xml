<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.najeungyoon.find_band_room.repository.StudioDAO">

    <select id="findAll" resultType="org.najeungyoon.find_band_room.model.StudioDTO">
        select id, name, placeKey
        from studio
    </select>

</mapper>